# CS1XC3 — Major Lab 5: Git, multifile C, sorting, `make`

---

## What to try

**1. Normal array**  
Use the existing test case to run the program.

**2. One-element array (segfault)**  
Change the arr so there is only one value in the array (also change N). Run the program.

Debug **`selection_sort`** in **`SelectionSort.c`** to fix the segmentation fault

---

## Build

```bash
make clean && make
./lab
```

