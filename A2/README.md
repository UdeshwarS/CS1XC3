# Assignment 2


## Instructions

- For this assessment, you will be required to submit your work through the department's gitlab server, as you have been doing with your labs and prior assignment.
- Complete the questions using the code templates provided. Do not change function names, variable names, or input and output types of anything in the provided template.
- Your assignment will be graded with an autograder running on `cs1xc3.cas.mcmaster.ca`. You are therefore encouraged to develop and test your code within the server. See the `ssh-setup-vscode` page under the Environment Setup Instructions module on A2L for a guide on how to setup VS Code so you can write your code directly on the server, if you don’t want to use vim/nano.
- Unless otherwise specified by a question, do not include any functions like `scanf` that require user input in your implementations of the template functions. This will cause the autograder to hang and result in a grade of zero for that question. Use your main() function to test user inputs if necessary.
- **Compile and run your code!** You are not explicitly instructed to compile and run your code, but it is expected that you are doing this as you go to ensure your code is working properly. If the code doesn’t compile or run, the autograder result in a zero for that question.


🚨 **ADD+COMMIT+PUSH your changes frequently! Backup your work with git! This will be your only reminder.**

You should make a habit of committing and pushing your code so that your work is not lost. Use helpful commit messages for yourself, such as “fundamentals: q1”, “game: fix loop bug”. 


## Final Submission

After you have completed all activities and you are satisfied with your results, you MUST submit your final submission by git add, commit, push, with a commit message that says exactly “assignment 2 final submission”, before 11:59 PM on the day of the deadline. 

```bash
$ git add * # Mark changes for commit
$ git commit -m "assignment 2 final submission" # Record changes
$ git push # Send changes to GitLab
```

