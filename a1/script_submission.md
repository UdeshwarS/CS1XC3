# V. Write a Script

### Q1. Provide the command you used to change the permissions of `days_ago.sh` so you can execute it. Provide the output of `ls -l` to confirm the permissions are correct.

```bash
$ # your command
$ ls -l days_ago.sh
# output of ls -l days_ago.sh
```

### Q2. Provide the series of commands that produces the output of the text block above.

```bash
bash # ...
bash # ....
# ...
```

### Q3. Complete the grep command

```bash
grep -Po #your completed command
```

### Q4. Provide the pipeline that removes consecutive duplicates and the resulting output

```bash
$ # your pipeline
# your output
```

### Q5. Provide the pipeline that removes all duplicate lines and the resulting output

```bash
$ # your pipeline
# your output
```

### Q6. Provide the command that changes the permissions of `future_date.sh` and provide the output of ls -l to confirm the permissions are set correctly.

```bash
$ # your command
$ ls -l future_date.sh
# paste output
```

### Q7. Provide the series of commands that produces the output of the text block above.

```bash
bash # ...
bash # ....
# ...
```

## Submission:

Should have the `days` directory with the file `days_ago.sh`, `future_date.sh`, `days_out.txt`, `days_ahead.txt`